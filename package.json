{
  "name": "knoqgen",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "wrangler dev",
    "deploy": "wrangler deploy",
    "db:push": "echo 'Run schema.sql against your Neon database via the Neon SQL Editor or psql'"
  },
  "dependencies": {
    "@neondatabase/serverless": "^0.9.0",
    "hono": "^4.4.0"
  },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20240512.0",
    "typescript": "^5.4.0",
    "wrangler": "^4.83.0"
  }
}
